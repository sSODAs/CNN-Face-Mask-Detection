# CNN-Face-Mask-Detection
A complete face mask detection system built using a custom-trained Convolutional Neural Network (CNN) with advanced image preprocessing and real-time inference support.  This repository demonstrates the full pipeline — from dataset preparation and model training to evaluation and webcam-based deployment.
